# Full-matrix VM E2E — findings log

Running log for the two-VM matrix run (`cx-e2e-sup` supervisor mode, `cx-e2e-nosup` regular mode).
One row per defect found while the matrix runs: what failed, the evidence, where the fix landed.
Harness bugs and product bugs are labelled, because only one of the two ships to the fleet.

| # | Gate | Symptom (verbatim where possible) | Root cause | Kind | Fix / status |
| --- | --- | --- | --- | --- | --- |
| F15 | P3 deploy (invalidates P3/P4/P5) | `clr2app` stayed in `CX_IIS_SERVICES` even though the guest's staged classifier demonstrably returned `Unsupported` for it (`Get-IISServiceMap` in the guest: `clr2app … instr=Unsupported`) | Found by code inspection, not guesswork: `deploy.bat` runs `"%~dp0Install-Agent.ps1"` — the payload **next to itself**. The harness staged the fresh payload into `C:\cx-deploy\deploy\` but put `deploy.bat` in the PARENT `C:\cx-deploy\`, where this guest had an inherited payload from its `cx-fleet-test` past (`C:\cx-deploy\pkg\install-agent.log` dated 7/21). Every deploy in the matrix therefore ran the guest's OLD scripts, so P3/P4/P5 described last month's code — including the CLR-2 claim, and very likely the zero Node telemetry (that payload predates the Node work). | harness | Fixed three ways: the stage root moved to `C:\cx-matrix` so an inherited `C:\cx-deploy` cannot shadow it; `deploy.bat`/`uninstall.bat` are invoked from INSIDE the payload directory; and P3 now writes a marker containing this checkout's git HEAD next to the scripts and asserts the guest reads it back, plus asserts nothing shadows at the stage root. A run can no longer silently test the wrong code. |
| F14 | P10 uninstall | `FAIL uninstall.bat exited 0 -> exit=1: Attempting start...` while the guest's own output read `iisDeinstrumented=True nodeDeinstrumented=True services=[] … uninstall.bat exit code: 0` | `Invoke-GuestFile` returned VBoxManage's exit code, which answers a different question. The step succeeded but the transport gave up reading its output (`Reading from guest process … failed: VERR_TIMEOUT`), so VBoxManage exited non-zero and a clean uninstall was reported as a failure. | harness | Fixed — the guest's own exit code is echoed as a `CX_GUEST_EXIT=<n>` marker and parsed back; VBoxManage's code is kept only as a fallback and a `TransportTimedOut` flag is surfaced, so transport trouble and guest failure can no longer look alike. |
| F13 | P9 reboot | `FAIL opampsupervisor Running after reboot with no intervention -> got [Stopped]` and `collector child back after reboot -> otelcol processes=0` | Not a product failure. `sc qc` showed `START_TYPE: AUTO_START (DELAYED)` and `sc qfailure` showed RESTART at 30s/60s/120s with RESET_PERIOD 86400 — the resilience settings were correctly applied. A **delayed** auto-start service deliberately starts about two minutes after boot, while `guestcontrol` answers much sooner, so the harness's fixed 90-second wait measured a service that had not been asked to start yet. | harness (assertion was wrong) | Fixed — P9 now POLLS for up to 7 minutes and prints how long the service took, since that gap is the host's real blind period. It additionally asserts the three settings directly (delayed-auto, recovery actions, and `failureflag=1` — the last matters because the supervisor exits CLEANLY and the SCM skips recovery for a non-crash exit), so a regression in the configuration is caught as itself rather than as a flaky reboot. |
| F12 | P7 telemetry | `FAIL infra labels verified in Coralogix -> Verify-CoralogixInfraLabels.ps1 : Cannot process argument transformation on parameter 'LookbackMinutes'. Cannot convert value "staticwc" to type "System.Int32"` — the verifier died before running a single query | A `[string[]]` parameter reached through `powershell -File` must be ONE comma-separated token. The harness spread the array (`-MustNotContain clr2app arrproxy staticwc …`), so only the first element bound to `-MustNotContain` and the remainder fell onto the following POSITIONAL parameters — `staticwc` landing on `-LookbackMinutes`. | harness | Fixed — array parameters are joined with commas. Note the failure mode: an argument bug in the gate reads exactly like a telemetry failure, which is why the assertion prints the verifier's own output. |
| F11 | P7 telemetry | Both runs printed a normal summary after P6 — and P7, P9 and P10 had simply never executed. stderr showed `NativeCommandError` at the `& powershell … -File $verifier` line | The same PS 5.1 trap `VBoxSoft` guards against, hit somewhere it was not expected: under `$ErrorActionPreference='Stop'`, ANY stderr write by a native command — here `powershell.exe` running the Coralogix verifier — becomes a **terminating** error. The verifier's first warning line aborted the whole `try` block, and because the summary is printed in `finally`, the run looked like it had ended normally. A silently-skipped telemetry gate is the worst possible failure for this harness. | harness | Fixed — `Invoke-HostScript` runs host-side gates with stderr demoted, returning code + output and never throwing. Verified with a probe script that writes to stderr and exits 3: the caller now survives, captures `code=3`, and continues. The Node gate also now targets the fork app explicitly (cluster is a documented known-open). |
| F10 | P5 naming | `FAIL clr2app is NOT claimed (CLR 2 is unsupported by the profiler) -> claimed: … coreweb-net8, coreweb-net8-oop, coreweb-net6, fw48app, **clr2app**` — on both VMs, in both install modes | `Get-IISAppInstrumentability` keyed on *whether* a pool loads a CLR (`PoolClrLoads`), which is `$true` for v2.0 and v4.0 alike. A Framework app in a `managedRuntimeVersion=v2.0` pool therefore came out **Supported**, got an `OTEL_SERVICE_NAME`, and was claimed in `CX_IIS_SERVICES` — but the OpenTelemetry .NET auto-instrumentation supports Framework 4.6.2+, so it can never report. The host advertises a Service that is permanently silent, which reads as an outage rather than as an unsupported runtime. | **product** | Fixed — the verdict now consults the CLR VERSION: Framework + CLR 2 is `Unsupported`, with its own finding code `FRAMEWORK_CLR2_NOT_INSTRUMENTABLE` at info severity (the app is healthy; only instrumentation is impossible) and a reason that says to move it to a v4.0 pool. Core + v2.0 stays `Misconfigured` — Core never uses the desktop CLR. Pinned by 6 new assertions in `test/Test-ResolveIISAppRuntime.ps1`, including that `v4.0.30319` is not mistaken for CLR 2. Needs VM re-verification: the in-flight runs used the pre-fix code. |
| F9 | P2 shapes | `FAIL clr2 answers HTTP 200 locally -> expected [200], got [404]` — a 404, not a 500, for a file that is present on disk | Installing the .NET 3.5 feature gives the host a CLR 2 runtime but does **not** map `*.aspx` to ASP.NET 2.0 in IIS. In a `v2.0` pool the request is then served as a static file with no handler, and IIS answers 404 — which reads as a missing file rather than an unregistered framework. | harness | Two changes: prereqs now run `aspnet_regiis -i -enable` from `Framework642.0.50727` when it exists, and the 404 case is a NOTE rather than a failure. This shape's contract is classification (classified, not claimed, no telemetry — asserted in P5/P7), and whether CLR 2 is mapped at all is a property of the host. |
| F8 | P2 shapes | `FAIL PM2 per-user fork shape provisioned -> exit=20: [shape] pm2 start shape-user-fork` | Same root cause as F7: the PM2 apps are the same fixtures that `require('pino')`, so with no `node_modules` staged, `pm2 start` could not keep them up either. | harness | Fixed by F7's staging change; re-verified in the follow-up run rather than assumed. |
| F7 | P2 shapes | `FAIL node-as-service is Running -> expected [Running], got [Stopped]`. The winsw wrapper log showed `Started 3292` and the error log `Error: Cannot find module 'pino' … code: 'MODULE_NOT_FOUND'`; running the app by hand in the guest reproduced it immediately | The Node app fixtures `require('pino')`, and the harness staged the app SOURCE but never their `node_modules`. `Dockerfile.nodeshapes` solves this by copying `nodeapp/node_modules` to the PARENT of the app directories (Node resolves upward, so one copy serves cjs/esm/mjs) — the VM path simply did not mirror it. Would have broken the PM2 shapes for the same reason. | harness | Fixed — the shared `node_modules` is staged to the same parent location the Dockerfile uses. The service itself was installed and started correctly, so the instrumenter under test was never implicated. |
| F6 | P1 staging | `[transport] recursive copy of …\otel-node reported failure; falling back to per-file` — and the per-file path was still grinding through a 71 MB tree of thousands of `node_modules` files with no end in sight | `guestcontrol copyto` copies file by file, and the trees this harness stages (otel-node, npm-global) are the worst possible shape for it. `--recursive` refused (it will not write into an existing target), so the fallback took over and turned staging into an hours-long step. | harness | Fixed — `Copy-DirToGuestAsZip`: compress on the host, one transfer, expand in the guest, with the per-file path kept as a fallback. Also removes the partial-copy failure mode where a tree lands a few files short and only fails later as a confusing "module not found" inside the guest. |
| F5 | P0 provision | Both runs failed `guestcontrol reaches the guest` after a 20-minute wait, and the VMs sat in `VMState="livesnapshotting"` with **no snapshot ever appearing** and no disk being written. Killing the client then left both machines registered as `<inaccessible>`, recoverable only by unregistering and re-cloning | P0 took its baseline snapshot with `snapshot take --live` on a running guest. On this host that state persists indefinitely, and while it persists `guestcontrol` stops answering - so the harness killed its own transport, then corrupted the machine XML when the interrupted operation was cut short. | harness | Fixed twice over: `New-VmSnapshot` now pauses, snapshots, and resumes instead of using `--live`, and P0 does not snapshot at all unless `-TakeSnapshot` is passed - the matrix never needed it. When it is passed, P0 additionally asserts the guest still answers afterwards, so this failure mode can never again be silent. |
| F4 | P1 prerequisites | Caught before it fired: the guest prerequisite report built `Node = (if (Test-Path …) { … } else { '' })` | In Windows PowerShell 5.1 the bare parenthesised `(if …)` **parses** — so a syntax check passes — and then fails at runtime with `The term 'if' is not recognized as the name of a cmdlet`, because it is read as a command invocation. The report would have said node was absent and failed a prerequisite that was actually satisfied. Only found by evaluating the form rather than parsing it. | harness | Fixed — `$(if …)` subexpression. The two in-flight runs were stopped and relaunched, because a PowerShell process has already loaded its script and cannot pick up the edit. |
| F3 | P0 transport | Both runs died after one assertion: `Cannot bind argument to parameter 'VmName' because it is an empty string`, and the summary printed `full-matrix (supervisor, )` with the VM name blank — despite `-VmName cx-e2e-c1` being passed | `VmLoop.Common.ps1` declared its transport state as `$script:VmName = $null`. Dot-sourcing runs in the CALLER's scope, so the shared file wiped the runner's own `-VmName` parameter before it was ever used. The same collision applied to `$User`, `$GuestStage` and friends. | harness | Fixed — all shared state is prefixed (`$script:VmlName`, `$script:VmlGcBase`, …), including the splat form `@script:GcBase`, which has no `$` and so survived the first rename pass. Verified with a probe script that dot-sources the file into a scope where `$VmName`/`$User` are already set. |
| F2 | pre-flight unit | `Merge-CxNodeOptions` returned `--require C:/cx/otel-node/register.js` **twice** when merging a value it had already written | The prior-hook check recognised our bootstrap only if its path matched `auto-instrumentations-node\|opentelemetry`. The default install prefix contains both by coincidence, so the bug is invisible there — but a vendored or differently-prefixed install (`-InstallPrefix C:\otel`) is not recognised, and every re-deploy appends another `--require`, loading the SDK twice. The function's own docstring promised this could not happen. | **product** | Fixed — recognition is now by exact normalised target (`file:///C:/x` = `C:\x` = `C:/x`), with the marker rule kept for values written by older versions, plus a new `-OwnedTargets` parameter so a caller can declare artifacts absent from the current bootstrap (an ESM→CommonJS switch would otherwise leave our stale `--experimental-loader` forever). Both callers updated. Pinned by 8 new assertions in `test/Test-ServiceInstrumenters.ps1`, incl. the app-keeps-its-own-`--require` false-positive case. |
| F1 | P0 provision | `Run-TestVM.ps1 -Action Unattended` died before doing anything: `Join-Path : Cannot bind argument to parameter 'Path' because it is an empty string` at line 35 (`$IsoPath` default) | `$PSScriptRoot` is **not populated while param-block defaults are evaluated**, so every default built from it (`$IsoPath`, `$PackagePath`, `$AdditionsIso`) collapsed to an empty path. The documented invocation `-Action Unattended` could never have worked without passing `-IsoPath` explicitly. The repo's other scripts already repair this after the param block; this one did not. | harness | Fixed — paths repaired after the param block, same `$here` pattern as `Install-CoralogixSupervisor.ps1` / `Run-SupervisorVmLoop.ps1`. |

## Notes on method

- A gate that passes after a fix but was never observed failing is not evidence: each row above
  records the failing observation first.
- Assertions are only loosened when the assertion itself was wrong, and then the reason is written
  in this table rather than left in the diff.
- Anything that turns out to be a genuine product limitation stays here as a known-open with its
  evidence instead of being quietly dropped from the matrix.
